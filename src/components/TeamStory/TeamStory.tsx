import * as Styled from "./styled";

type TeamStoryPropsType = {
  iconBg: string;
  teamTitle: string;
  heading: string;
  desc: string;
};

const TeamStory = ({
  iconBg,
  teamTitle,
  heading,
  desc,
}: TeamStoryPropsType) => {
  return <Styled.Wrapper></Styled.Wrapper>;
};

export default TeamStory;
